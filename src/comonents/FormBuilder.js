import React from 'react';

const FormBuilder = (props) => {
    const {fields} = props

    return (
        <div>
            <form>
                
            </form>
        </div>
    );
};

export default FormBuilder;